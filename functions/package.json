{
  "name": "smart-expense-manager-functions",
  "description": "Firebase Cloud Functions for Smart Expense Manager",
  "main": "index.js",
  "private": true,
  "engines": {
    "node": "22"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-functions": "^7.3.0"
  }
}
